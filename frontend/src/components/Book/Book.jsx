import React from "react";

const Book = () => {
  return (
    <div>
      <h4>
        {/* Question: Would it be better to use Google Books embedded viewer API here? */}
        This is a book. It should display the title, the author , other metadata
      </h4>
    </div>
  );
};

export default Book;
